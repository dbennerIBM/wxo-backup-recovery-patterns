/**
 * Popup panel — entry point.
 *
 * Sub-Task 1: Scaffold only.
 * Full implementation will be added in Sub-Task 5 (Popup Panel UI).
 */

const statusEl = document.getElementById("status");
if (statusEl) {
  statusEl.textContent = "Ready (scaffold)";
}
